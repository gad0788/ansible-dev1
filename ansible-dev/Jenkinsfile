pipeline {
    agent{
        label 'ansible-master'
    }
    stages {
        stage('Check working directorygt') {
            steps {
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
