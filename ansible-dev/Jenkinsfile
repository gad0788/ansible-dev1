pipeline {
    agent{
        label 'ansible-master'
    }
    stages {
        stage('Check working dir') {
            steps {
                sh 'pwd'
                sh 'ls'
            }
        }
        stage('Checking time on server') {
            steps {
                sh 'uptime'
               
            }
        }
    }
}
